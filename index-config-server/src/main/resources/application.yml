spring:
  application:
    name: index-config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/1460595002/TrendConfig1
          searchPaths: src/config
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/